#!/bin/bash

# declare two variables having same value
S1="Bash"
S2="Bash"
# Compare both using if else statement
if [ S1 = S2] ; then
	echo "Both Strings are equal"
else
	echo "Strings are not equal"
fi
